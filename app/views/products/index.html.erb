<h1>All Products</h1>

<ul>
  <% (@products || []).each do |product| %>
    <li>
      <%= link_to (product.title || "Untitled"), product_path(product) %><br>
      Price: <%= product.price %><br>
      Stock: <%= product.stock_quantity %>
    </li>
  <% end %>
</ul>